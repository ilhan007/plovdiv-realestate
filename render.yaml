services:
  - type: web
    name: plovdiv-realestate
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: node server/index.mjs
    envVars:
      - key: NODE_ENV
        value: production
